{% extends 'base.html.twig' %}

{% block title %}Horaire{% endblock %}

{% block body %}
    <h1>Horaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horairedujour.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ horairedujour.Jour }}</td>
            </tr>
            <tr>
                <th>MorningStartTime</th>
                <td>{{ horairedujour.morningStartTime ? horairedujour.morningStartTime|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>MorningEndTime</th>
                <td>{{ horairedujour.morningEndTime ? horairedujour.morningEndTime|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>AfterStartTime</th>
                <td>{{ horairedujour.afterStartTime ? horairedujour.afterStartTime|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>AfterEndTime</th>
                <td>{{ horairedujour.afterEndTime ? horairedujour.afterEndTime|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_horaire_index') }}">back to list</a>

    <a href="{{ path('app_horaire_edit', {'id': horairedujour.id}) }}">edit</a>

    {{ include('horaire/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_horaire_delete', {'id': horairedujour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ horairedujour.id) }}">
    <button class="btn">Delete</button>
</form>
